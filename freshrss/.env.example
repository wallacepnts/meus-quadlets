TZ=America/Sao_Paulo

# Frequência de atualização automática dos feeds (formato de minuto do
# cron, ex.: 1,31 = a cada 30 min; */20 = a cada 20 min). Vazio = sem
# atualização automática, só manual (Atualizar feeds na UI).
CRON_MIN=1,31
