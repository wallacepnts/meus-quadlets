# PUID/PGID do usuário que roda o Podman (mesmo dono de MEDIA_DATA_DIR,
# senão os apps não conseguem escrever nela) — conferir com `id -u`/`id -g`
# e ajustar os valores abaixo antes de copiar.
PUID=1000
PGID=1000
TZ=America/Sao_Paulo
