# Todas as variáveis são opcionais — sem nenhuma delas a imagem usa os
# defaults (NGINX_PORT=80, WEB_APP_PORT=3000, MENU_VERSION=mais recente).
# Deixado vazio de propósito; ver quadlet/netbootxyz.container pro mapeamento
# de portas do host.

# Prende o menu do netboot.xyz numa versão específica em vez da mais
# recente disponível (ex.: 2.0.84). Sem isso, o menu builder é atualizado
# sozinho a cada start, independente da tag da imagem.
# MENU_VERSION=2.0.84
